import { RechercheMembrePipe } from './recherche-membre.pipe';

describe('RechercheMembrePipe', () => {
  it('create an instance', () => {
    const pipe = new RechercheMembrePipe();
    expect(pipe).toBeTruthy();
  });
});
